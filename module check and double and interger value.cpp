#include <bits/stdc++.h>
using namespace std;
int main()
{
    // double x = 10.5, y = 3.2; // that is not correct, because double variable have not compiler.
    // double result = x % y;
    int p = 10, q = 55;
    int result2 = q % p;
    cout << result2 << endl;
    // cout << result << endl;
    // i can use nagative value then check the modulo operator
    int w = -2, r = 4;
    int c = r % w;
    cout << c << endl;
}